🧪 Add tests for get() function in state.js

🎯 What: Added unit tests for the untested `get` function in `public/js/state.js` to ensure reliable retrieval of state properties and safe state cloning.
📊 Coverage:
- Retrieval of existing keys.
- Retrieval of non-existent keys (returns undefined).
- Shallow copying of the complete state when called without a key.
✨ Result: Improved codebase reliability by fully covering the `get` function and establishing a testing pattern for `state.js` using `node:test`.
